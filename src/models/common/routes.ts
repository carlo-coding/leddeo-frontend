export enum PublicRoutes {
  LOGIN = "login",
  SIGNUP = "signup",
  UPLOAD = "upload",
  EDITOR = "editor",
  PLANS = "plans",
  OPTIONS = "options",
  SRT_UPLOAD = "srt-upload",
  FAQS = "faqs",
  LANDING = "home",
  TERMS = "terms",
}

export enum PrivateRoutes {
  PRIVATE = "private",
  PROFILE = "profile",
  DASHBOARD = "dashboard",
  HISTORY = "history",
}
